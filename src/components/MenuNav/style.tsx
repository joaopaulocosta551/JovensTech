import styled from 'styled-components';

export const Nav = styled.nav`

    display: flex;
    flex-direction: row;
    max-width: 80rem;
    width: 100%;
    align-items: center;
    margin-top: 1.25rem;

`;
